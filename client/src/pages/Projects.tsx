import { PageHero, PageShell } from "@/components/PageShell";
import { INITIATIVES, OPEN_SQUADS } from "@shared/content";
import { ArrowRight, ArrowUpRight, CheckCircle2, Code2, GitBranch, Layers3, Rocket, ShieldCheck, UsersRound } from "lucide-react";
import { Link } from "wouter";

export default function ProjectsPage() {
  return (
    <PageShell>
      <PageHero eyebrow="Flagship R&D platform | Builder + Squad Pro" title={<>Build systems that <span className="brand-gradient-text">leave the classroom.</span></>} description="Collaborative engineering teams tackle open problems across aerospace, astrophysics, numerical simulation, research tooling, and K–12 outreach. Every squad ships a verifiable artifact—not just an assignment." />
      <section className="container px-4 py-12 sm:px-6 sm:py-16"><div className="overflow-hidden rounded-3xl border border-primary/20 bg-card"><div className="grid lg:grid-cols-[1.15fr_0.85fr]"><div className="p-6 sm:p-8"><div className="flex items-center gap-2"><span className="rounded-full bg-primary/10 px-2.5 py-1 font-mono text-[0.58rem] uppercase tracking-[0.1em] text-primary">★ Flagship Platform</span><span className="rounded-full bg-success/10 px-2.5 py-1 font-mono text-[0.58rem] uppercase tracking-[0.1em] text-success">Active build</span></div><h2 className="mt-5 font-display text-4xl font-bold tracking-[-0.045em] text-foreground">AeroForge AI Simulation Workstation</h2><p className="mt-4 max-w-2xl text-sm leading-7 text-muted-foreground">Browser-based engineering research workstation with 40+ physics solvers across CFD aerodynamics, structural FEA, and orbital mechanics. The team is building an open, explainable alternative to black-box classroom demos.</p><div className="mt-7 flex flex-wrap gap-3"><Link href="/aeroforge" className="press inline-flex items-center gap-2 rounded-full bg-[image:var(--grad-brand)] px-4 py-2.5 font-mono text-[0.65rem] font-semibold text-[#090710]">Launch AeroForge <ArrowUpRight className="h-3.5 w-3.5" /></Link><Link href="/pricing" className="press inline-flex items-center gap-2 rounded-full border border-border px-4 py-2.5 font-mono text-[0.65rem] text-foreground hover:border-primary/45 hover:bg-primary/10">Join a Squad <UsersRound className="h-3.5 w-3.5 text-primary" /></Link></div></div><div className="grid grid-cols-2 border-t border-primary/15 bg-[var(--surface-2)] lg:border-l lg:border-t-0">{[{ icon: Layers3, label: "Solvers", value: "40+ Numerical Physics" }, { icon: Rocket, label: "Aerodynamics", value: "Spalart-Allmaras CFD" }, { icon: GitBranch, label: "Astrodynamics", value: "N-Body & Keplerian" }, { icon: Code2, label: "License", value: "Open system core" }].map(({ icon: Icon, label, value }) => <div key={label} className="border-b border-r border-primary/12 p-5 sm:p-6"><Icon className="h-4 w-4 text-primary" /><p className="mt-4 font-mono text-[0.57rem] uppercase tracking-[0.09em] text-muted-foreground">{label}</p><p className="mt-1 text-sm font-semibold leading-5 text-foreground">{value}</p></div>)}</div></div></div></section>
      <section className="border-y border-border bg-[var(--surface)] px-4 py-16 sm:px-6 sm:py-20"><div className="container"><div className="max-w-2xl"><p className="eyebrow">Active initiatives</p><h2 className="mt-3 font-display text-4xl font-bold tracking-[-0.045em] text-foreground">Four build tracks. Real deliverables.</h2><p className="mt-3 text-sm leading-7 text-muted-foreground">Each initiative is intentionally small enough for students to own, and complete enough to survive public scrutiny.</p></div><div className="mt-10 space-y-5">{INITIATIVES.map((initiative, i) => <article key={initiative.id} className="overflow-hidden rounded-2xl border border-border bg-card"><div className="grid gap-5 p-5 sm:p-6 lg:grid-cols-[1.1fr_0.8fr_0.9fr] lg:gap-8"><div><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-primary/10 px-2 py-1 font-mono text-[0.55rem] uppercase tracking-[0.09em] text-primary">{initiative.domain}</span>{initiative.flagship && <span className="inline-flex items-center gap-1 rounded-full bg-gold/10 px-2 py-1 font-mono text-[0.55rem] uppercase tracking-[0.08em] text-gold">★ Flagship Platform</span>}</div><h3 className="mt-4 font-display text-2xl font-bold text-foreground">{initiative.title}</h3><p className="mt-3 text-sm leading-6 text-muted-foreground">{initiative.description}</p><p className="mt-4 font-mono text-[0.6rem] text-primary">{initiative.stack}</p></div><div><div className="grid grid-cols-2 gap-3"><SmallStat label="Level" value={initiative.level} /><SmallStat label="Duration" value={initiative.duration} /></div><div className="mt-5"><div className="flex items-center justify-between font-mono text-[0.57rem]"><span className="text-muted-foreground">Build Progress</span><span className="text-primary">{initiative.progress}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-secondary"><div className="h-full rounded-full bg-[image:var(--grad-brand)]" style={{ width: `${initiative.progress}%` }} /></div><p className="mt-3 inline-flex items-center gap-1.5 font-mono text-[0.6rem] text-muted-foreground"><UsersRound className="h-3 w-3 text-primary" />{initiative.members}</p></div></div><div><p className="font-mono text-[0.56rem] uppercase tracking-[0.1em] text-muted-foreground">Build Journey</p><div className="mt-3 flex flex-wrap items-center gap-1.5">{initiative.journey.map((step, stepIndex) => <span key={step} className="inline-flex items-center gap-1"><span className="rounded-md bg-secondary px-2 py-1.5 font-mono text-[0.54rem] text-foreground"><span className="text-primary">{String(stepIndex + 1).padStart(2, "0")}</span> {step}</span>{stepIndex < initiative.journey.length - 1 && <ArrowRight className="h-2.5 w-2.5 text-primary" />}</span>)}</div><p className="mt-5 font-mono text-[0.56rem] uppercase tracking-[0.1em] text-muted-foreground">What you ship</p><div className="mt-2 flex flex-wrap gap-1.5">{initiative.ships.map(ship => <span key={ship} className="rounded-md border border-primary/15 bg-primary/6 px-2 py-1 font-mono text-[0.54rem] text-muted-foreground">{ship}</span>)}</div><Link href="/pricing" className="press mt-5 inline-flex items-center gap-1.5 rounded-full bg-primary px-3.5 py-2 font-mono text-[0.6rem] font-semibold text-primary-foreground">Join Squad <ArrowRight className="h-3 w-3" /></Link></div></div></article>)}</div></div></section>
      <section className="container px-4 py-16 sm:px-6 sm:py-20"><div className="flex flex-col gap-3 border-b border-border pb-6 sm:flex-row sm:items-end sm:justify-between"><div><p className="eyebrow">Open squads</p><h2 className="mt-3 font-display text-4xl font-bold tracking-[-0.045em] text-foreground">There is a sprint with your name on it.</h2></div><Link href="/pricing" className="font-mono text-[0.64rem] text-primary hover:text-gold">Squad Pro membership details →</Link></div><div className="mt-6 grid gap-4 md:grid-cols-2">{OPEN_SQUADS.map(squad => <article key={squad.domain} className="rounded-2xl border border-border bg-card p-5 transition-colors hover:border-primary/40"><div className="flex items-center justify-between"><span className="font-mono text-[0.59rem] uppercase tracking-[0.1em] text-primary">{squad.domain}</span><span className="rounded-full bg-secondary px-2.5 py-1 font-mono text-[0.54rem] text-muted-foreground">{squad.level}</span></div><p className="mt-4 font-mono text-[0.6rem] text-gold">{squad.stack}</p><p className="mt-4 text-xs leading-5 text-muted-foreground"><span className="font-mono text-[0.56rem] uppercase tracking-[0.08em] text-foreground">Current Sprint</span><br />{squad.sprint}</p><div className="mt-5 flex items-center justify-between border-t border-border pt-4"><span className="inline-flex items-center gap-1.5 font-mono text-[0.58rem] text-muted-foreground"><UsersRound className="h-3 w-3 text-primary" />{squad.builders}</span><Link href="/pricing" className="press inline-flex items-center gap-1 rounded-full border border-primary/35 px-3 py-1.5 font-mono text-[0.58rem] text-primary hover:bg-primary hover:text-primary-foreground">Join <ArrowRight className="h-3 w-3" /></Link></div></article>)}</div></section>
    </PageShell>
  );
}

function SmallStat({ label, value }: { label: string; value: string }) {
  return <div className="rounded-xl border border-border bg-secondary/35 p-3"><p className="font-mono text-[0.53rem] uppercase tracking-[0.08em] text-muted-foreground">{label}</p><p className="mt-1 text-xs font-medium leading-5 text-foreground">{value}</p></div>;
}
